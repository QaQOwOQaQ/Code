如果一个类重载了运算符()，那么这个类的对象就成为了**函数对象**

一般在()中给出参数的前面都是都是函数指针或者函数
例如sort(args)中sort就是一个函数
但在重载()之后可以在对象的后面加上（args）
例如：obj(args) <=在重载()后=> obj.operator()(args)
obj(args)看上出像一个函数调用的形式，实际上也执行了一个函数调用
但是obj不是一个函数而是一个对象，所以我们把obj称为函数对象

